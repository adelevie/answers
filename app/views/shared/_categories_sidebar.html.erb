<div class="sidebar">
  <div class="sidebar-unit">
    <h4 class="side-head">ALL CATEGORIES</h4>
    <div class="sidebar-content">
       <ul>
        <% categories.sort_by {|c| c.name}.each do |c| %>
          <%= content_tag( :li, link_to(c.name, c)) %>
        <% end -%>
       </ul>
    </div>
  </div>
</div>
